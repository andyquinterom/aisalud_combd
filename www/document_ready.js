
$(document).ready(function(){

  /*

  $('.botones_convertir_fila_1').each(function(){  
    var highestBox = 0;
    $('button', this).each(function(){
      if($(this).height() > highestBox) {
        highestBox = $(this).height(); 
      }
    });
    $('button',this).height(highestBox);
  }); 
    
  $('.botones_convertir_fila_1').each(function(){
    var highestBox = 0;
    $('button', this).each(function(){
      if($(this).height() > highestBox) {
        highestBox = $(this).height(); 
      }
    });  
    $('button',this).height(highestBox);
  }); 

  */

});
